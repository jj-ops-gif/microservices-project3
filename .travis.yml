language: node_js
node_js:
  - 16

services:
  - docker

# Pre-testing installs
install:
  - echo "nothing needs to be installed"

# Scripts to be run such as tests
before_script:
  - echo "no tests"

script:
  - docker --version # print the version for logging
  - docker-compose -f docker-compose-build.yaml build
  - docker tag udagram-api-feed 0987432592345457/udagram-api-feed:latest
  - docker tag udagram-api-user 0987432592345457/udagram-api-user:latest
  - docker tag udagram-frontend 0987432592345457/udagram-frontend:latest
  - docker tag udagram-reverseproxy 0987432592345457/udagram-reverseproxy:latest

# Tasks to perform after the process is successful. Formatting the Docker username and password as below enables you to programmatically log in without having the password exposed in logs.
after_success:
  - docker image ls
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_USERNAME" --password-stdin
  - docker push 0987432592345457/udagram-api-feed:latest
  - docker push 0987432592345457/udagram-api-user:latest
  - docker push 0987432592345457/udagram-frontend:latest
  - docker push 0987432592345457/udagram-reverseproxy:latest